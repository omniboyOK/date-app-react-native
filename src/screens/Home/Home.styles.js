import { StyleSheet } from "react-native";

const styles = () =>
  StyleSheet.create({
    container: {
      flex: 1,
      margin: 10,
    },
    list: { paddingBottom: 50 }
  });

export default styles;
