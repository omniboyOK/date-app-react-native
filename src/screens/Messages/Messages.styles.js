import { StyleSheet } from "react-native";

const styles = (theme) =>
  StyleSheet.create({
    screen: {
      flex: 1,
      backgroundColor: theme.colors.snowWhite,
      margin: 10,
    },
  });

export default styles;
